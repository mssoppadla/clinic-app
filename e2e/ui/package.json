{
  "name": "clinic-ui-e2e",
  "private": true,
  "description": "Real-user browser end-to-end tests (Playwright) — drives the actual pages through Caddy + the API, catching JS errors, broken flows and a down/502 backend that in-process API tests can't see.",
  "scripts": {
    "test": "playwright test",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
